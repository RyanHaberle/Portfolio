import { SearByNamePipe } from './sear-by-name.pipe';

describe('SearByNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SearByNamePipe();
    expect(pipe).toBeTruthy();
  });
});
